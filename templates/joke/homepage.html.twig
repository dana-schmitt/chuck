{% extends 'base.html.twig' %}

{% block title %}Welcome!{% endblock %}

{% block body %}
    <div class="w-4/5 m-auto text-center min-h-full h-screen">

        <div class="pb-8">
            <h1 class="uppercase font-bold text-5xl text-gray-800 pt-48 pb-8">
               Chuck Norris
            </h1>
            <span class="font-light text-gray-500 italic">
                Jokes Hub
            </span>
        </div>

        <section class="hero container  mx-auto pt-12 flex">
            <img src="{{ asset('images/chuck.png') }}" class="mx-auto h-64">
        </section>

            <div class="pt-12 flex items-center justify-around flex-wrap">
            <a href="/login" class="uppercase font-bold border border-gray-300 bg-yellow-400 text-gray-800 text-lg py-4 px-12 rounded transition transition-all shadow-xl hover:border hover:border-gray-300 hover:bg-indigo-400 hover:text-white">Login</a>

            <a href="/register" class="uppercase font-bold border border-gray-300 bg-yellow-400 text-gray-800 text-lg py-4 px-12 rounded transition transition-all shadow-xl hover:border hover:border-gray-300 hover:bg-indigo-400 hover:text-white">Register</a>
        </div>

    </div>
{% endblock %}
