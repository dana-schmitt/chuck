{% extends 'base.html.twig' %}

{% block title %}Welcome!{% endblock %}

{% block body %}
    <div class="w-4/5 m-auto text-center min-h-dvh">

        <div class="pb-8">
            <h1 class="uppercase font-bold text-5xl text-zinc-100 pt-12 pb-8">
               Chuck Norris
            </h1>
            <span class="font-light text-zinc-400 italic">
                Jokes Hub
            </span>
        </div>

        <section class="hero container  mx-auto pt-12 flex">
            <img src="{{ asset('images/chuck.png') }}" class="mx-auto h-64">
        </section>

            <div class="pt-12 flex items-center justify-around flex-wrap">
            <a href="/login" class="uppercase font-bold border border-zinc-700 bg-yellow-400 text-zinc-900 text-lg py-4 px-12 rounded transition transition-all shadow-xl hover:border-zinc-600 hover:bg-indigo-400 hover:text-white">Login</a>

            <a href="/register" class="uppercase font-bold border border-zinc-700 bg-yellow-400 text-zinc-900 text-lg py-4 px-12 rounded transition transition-all shadow-xl hover:border-zinc-600 hover:bg-indigo-400 hover:text-white">Register</a>
        </div>

    </div>
{% endblock %}
